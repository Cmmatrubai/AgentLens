{
  "name": "agentlens",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "agentlens": "tsx --conditions=development apps/cli/src/developmentEntry.ts",
    "build": "tsc -b --pretty false && pnpm --filter @agentlens/web build && pnpm build:desktop",
    "build:web": "pnpm --filter @agentlens/web build",
    "test": "vitest --run && pnpm test:desktop",
    "test:e2e": "playwright test --config apps/web/playwright.config.ts",
    "typecheck": "tsc -b --pretty false && pnpm --filter @agentlens/desktop typecheck",
    "desktop": "pnpm --filter @agentlens/desktop desktop",
    "dev:desktop": "pnpm --filter @agentlens/desktop dev",
    "preview:desktop": "pnpm --filter @agentlens/desktop preview",
    "build:desktop": "pnpm --filter @agentlens/desktop build",
    "test:desktop": "pnpm --filter @agentlens/desktop test"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
