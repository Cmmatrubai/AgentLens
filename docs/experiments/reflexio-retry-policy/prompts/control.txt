Implement `request_json` in `http_client.py` and make the public tests pass. Keep the existing interface and dependencies. The fixed evaluator covers normal success, recovery from a transient GET failure, a strict bounded-attempt failure for a persistently failing GET, immediate failure for a non-retryable client error, and no duplicate payment POST submission. Use the injected sleep callback so tests never perform real waits. Run the public tests, keep edits narrowly scoped, and finish with a concise summary of edits and test results.
