# Connect an OpenAI-compatible endpoint

In the Electron desktop app, open **Comparison → Generated insights → Settings**.

1. Set **API base URL** to the server's API prefix, for example `https://your-provider.example/v1` or `http://127.0.0.1:1234/v1`. These are examples, not configured services. Do not append `/chat/completions` or `/responses`; AgentLens appends the selected route.
2. Enter the exact **Analysis model ID** served by that endpoint.
3. Enter that provider's **API key**, or select **This endpoint does not require an API key**. The latter omits the Authorization header.
4. Expand **API compatibility**. Changing the base URL selects **Chat Completions**; choose **Responses** if that is the API your server implements.
5. Select the server's **JSON output support**: **JSON schema** requests strict structured output; **JSON object** requests JSON mode; **Prompted JSON** sends the schema in the prompt without a response-format field. AgentLens validates the same output shape and evidence references in every mode.
6. Expand **Response limits** if needed. Choose a supported reasoning effort, an output limit of 1,000–32,000 tokens, and a time limit of 30–300 seconds. **Off (if supported)** explicitly sends `none`; **Provider default** omits the reasoning override. Defaults remain provider-selected reasoning, 6,000 tokens and 90 seconds. Reasoning tokens may consume the same output allowance. Unsupported effort values are reported as errors, without automatic fallback.
7. Enable analysis and save. Review the destination and selected evidence with **Preview & generate**, then explicitly start the request.

An OpenAI-issued key is not required for another provider. Remote endpoints must use HTTPS. HTTP is accepted only for localhost, 127.0.0.1 and ::1. Redirects are rejected. URL credentials, query strings and fragments are rejected.

Keys are encrypted using the desktop OS's protection and associated with the complete normalized base URL. Changing the endpoint requires its own key when authentication is enabled; a key saved for another destination is not reused. Selecting no-key mode removes the saved key. Settings saves do not contact the server.

The endpoint, request format, JSON mode and analysis model are recorded with each new analysis. Changes to provider settings invalidate cached results and require a new evidence review. Unsupported requests are shown as errors; AgentLens does not silently retry with another API or formatting mode. A server may still reject a particular model, parameter or schema despite advertising an OpenAI-compatible API. No model list is fetched automatically.

The Chat Completions adapter uses non-streaming requests and the selected output-token limit. Response limits are shown in the evidence review and saved with each analysis. Changing them invalidates earlier consent and cached results for different settings. A provider may charge for incomplete requests; AgentLens never retries them automatically. Responses requests also set `store: false`; provider retention behavior is governed by the selected service. Analysis results and recordings remain saved locally. Reviewed evidence is sent only when an analysis or evidence-support review is explicitly started.

After generation, **Review evidence support** requests a separate AI assessment using the same endpoint, model, compatibility settings and response limits. Its dialog shows the saved draft, selected excerpts, exact task requirements, saved attempt facts and source details before consent. This additional request may incur a charge even if it fails. Consent and saved reviews are tied to the original draft, evidence, settings and review versions; changed inputs require another review. A failed review keeps the original draft accessible and does not promote its findings. The reviewer selects IDs from a server-built passage catalog for each claim. AgentLens checks complete original-text coverage and valid IDs, then copies the exact passages from the selected evidence. Task requirements, source details and attempt facts retain separate labels. Requirements describe requested behavior, not proof of implementation. These checks establish passage provenance; whether a passage supports its claim remains an AI judgment. **Inspect claim checks** keeps this detail available without expanding every claim on the main page.

Request formats were checked against the [OpenAI Chat Completions reference](https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create). Different implementations expose different capabilities; see, for example, [Ollama's compatibility documentation](https://docs.ollama.com/api/openai-compatibility). AgentLens's adapter has been verified with mock responses and an actual loopback HTTP test server. Live compatibility and finding quality with individual providers remain to be evaluated.

September 7 follow-up: TokenRouter GLM-5.3 completed one C01 analysis with Prompted JSON, low reasoning, a 24,000-token allowance and a 300-second limit. It returned three structurally valid findings in 206.406 seconds. This is one observed completion; semantic review found qualifications still needed, and broader quality and speed validation remain open. See [the test report](../qa/insight-engine/TOKENROUTER-LIVE.md).
